# Azure App Service startup command for the backend.
# Set this in Azure Portal → App Service → Configuration → General settings → Startup Command
# Or reference it in the az CLI deploy command.
python run.py
